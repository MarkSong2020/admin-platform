# 任务 A1：authz 包基础 —— DataScope 值对象 + ScopeType 5 范围枚举

你是 admin-platform 的无人值守实现 agent。**只做本任务，完成后停**（不要顺手做 A2/A3）。严格遵守 `CLAUDE.md` + `doc/standards/AI_CODING_RULES.md`：分层红线、**中文 docstring**、全函数/类 type hints、不引新第三方依赖。

## 背景

本仓在实现 P1 RBAC，机制 spec = `docs/specs/2026-06-05-p1.0-rbac-mechanism.md`（**先读 §4 data_scope**）。数据权限 5 范围（RuoYi 标准）：全部 / 自定义部门 / 本部门 / 本部门及以下 / 仅本人。

## 范围（只做这些文件）

新建数据权限的**类型层**（纯值对象，无 IO）：

1. `src/admin_platform/authz/__init__.py` —— 包声明（中文 docstring，一句话说明 authz 是 RBAC 横切基础设施包）。
2. `src/admin_platform/authz/scope.py`：
   - `ScopeType` 枚举（`enum.Enum`）：`ALL` / `CUSTOM_DEPT` / `SELF_DEPT` / `SELF_DEPT_AND_BELOW` / `SELF`，每个成员带中文注释说明语义。
   - `DataScope` —— **frozen dataclass**（`@dataclass(frozen=True)`），字段：`scope_type: ScopeType`、`user_id: int`、`dept_id: int | None = None`、`visible_dept_ids: frozenset[int] = frozenset()`。中文 docstring。
3. `tests/unit/test_authz_scope.py` —— 覆盖：每个 ScopeType 可构造；DataScope frozen（改字段抛 `FrozenInstanceError`）；默认值正确。

## 硬约束

- **不碰 `core/`、`db/`、`main.py`**（无写权限，别尝试）。
- `authz` 包**不 import `domains/` 或 `core/`**（它是被依赖方，纯类型，避免循环依赖）。
- `scope.py` 不 import SQLAlchemy / FastAPI（纯 Python 值对象）。

## 完成判据（自测后才算完）

- `make check` 全绿（ruff + pyright + pytest + import-linter）。
- 若 import-linter 因新 `authz` 包报错，在 `.importlinter` 加约束：`authz` 禁止 import `domains` 与 `core`（forbidden contract）。

完成后停。