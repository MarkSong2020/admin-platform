# 任务 A3：Permission/Menu Provider 抽象接口 + stub 实现

无人值守实现 agent。**只做本任务，完成后停**。遵守 `CLAUDE.md` + `AI_CODING_RULES`。读 spec `docs/specs/2026-06-05-p1.0-rbac-mechanism.md` §5（Q8：P1 **不缓存**，接口先冻结）。

## 背景

spec §5.2 要求预留权限/菜单 Provider 抽象 + 失效接口（P1 是直查、no-op 失效，P2 接 Redis 时填充）。A1 已有 `authz/scope.py` 的 `DataScope`。

## 范围

1. `src/admin_platform/authz/providers.py`：
   - `PermissionProvider`（`abc.ABC` 或 `typing.Protocol`）：
     - `get_user_permissions(user_id: int) -> frozenset[str]`
     - `get_effective_data_scope(user_id: int) -> DataScope`
     - `invalidate_user(user_id: int) -> None` / `invalidate_role(role_id: int) -> None` / `invalidate_all() -> None`（失效接口，先冻结）
   - `MenuProvider`：
     - 一个简单 `MenuNode` dataclass（id/name/path/perms 等最小字段，frozen 或普通）
     - `get_user_menu_tree(user_id: int) -> list[MenuNode]`
     - 同样的 `invalidate_*` 三件套
   - 中文 docstring 说明"P1 无缓存、invalidate 为 no-op 占位、P2 接缓存"。

2. `src/admin_platform/authz/stub_providers.py`：
   - 基于内存 dict 的 `StubPermissionProvider` / `StubMenuProvider`（供机制层单测用，**不依赖 DB / RBAC 表**）。`invalidate_*` 实现为 `pass`（no-op）。

3. `tests/unit/test_providers.py`：stub 的 get 返回预置数据、invalidate 不报错。

## 硬约束

- **不碰 core/db/domains**；`authz` 内部可互 import（`scope` / `data_scope`）。
- 中文 docstring + type hints；`make check` 要过 pyright（抽象方法签名完整）。

## 完成判据

- `make check` 全绿。

完成后停。