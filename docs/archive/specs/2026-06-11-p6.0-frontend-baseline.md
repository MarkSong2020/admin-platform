# P6.0 前端工程基线 + session/transport spike 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭好 P6 前端工程基线（脚手架 + 6 道 CI 门 + 本地提交门），并把最高风险的 `session.ts`（认证协调器）/ `transport.ts`（二进制）/ 动态路由 contract 前置 spike 到可测绿。

**Architecture:** 完全自建 Vue3+TS。后端先产 DB-free 确定性 OpenAPI 与 seed 页面组件真值源；前端用 `openapi-typescript` 生成类型，`api/session.ts` 作唯一认证协调器（内存 access + sessionStorage refresh + single-flight refresh + emitter 发 `sessionExpired` + 抛 `SessionExpiredError`），`api/transport.ts` 兜 multipart/blob/错误归一，`dependency-cruiser` 机检分层（含 `api→stores/router` 负向）。

**Tech Stack:** Vue 3.5 / TypeScript strict / Vite / Element Plus / Pinia / Vue Router 4 / openapi-typescript + openapi-fetch / Vitest + @vue/test-utils / Playwright / dependency-cruiser / husky + lint-staged + commitlint / pnpm 10 + Node 22。

**Spec:** `docs/specs/2026-06-11-p6-frontend-design.md`（v5 对抗收敛）。

---

## Task 1: 后端 DB-free 确定性 OpenAPI 导出脚本

**Files:**
- Create: `scripts/dump_openapi.py`
- Test: `tests/unit/test_dump_openapi.py`
- Reference: `src/admin_platform/main.py`（`create_app().openapi()`）、`src/admin_platform/core/config.py`（`get_settings` lru_cache、`auth_public_paths`、`app_name`）

- [ ] **Step 1: 写失败测试 —— 污染 env 不影响输出**

`tests/unit/test_dump_openapi.py`:

```python
"""dump_openapi 确定性契约：环境变量污染不得改变导出的 OpenAPI schema。"""
import json
import os

from scripts.dump_openapi import build_contract_openapi


def test_openapi_is_deterministic_under_env_pollution(monkeypatch):
    """固定 contract profile 下，污染影响 schema 的 APP_* 不改变输出。"""
    clean = build_contract_openapi()

    monkeypatch.setenv("APP_NAME", "polluted-name")
    monkeypatch.setenv("APP_ENVIRONMENT", "production")
    monkeypatch.setenv("APP_AUTH_PUBLIC_PATHS", '["/totally/wrong"]')

    polluted = build_contract_openapi()

    assert polluted == clean, "OpenAPI 导出受 env 污染，确定性被破坏"


def test_openapi_has_expected_shape():
    """导出的 schema 含 openapi 版本与 paths，且为可 JSON 序列化的 dict。"""
    schema = build_contract_openapi()
    assert schema["openapi"].startswith("3.")
    assert "/api/v1/auth/login" in schema["paths"]
    json.dumps(schema)  # 不抛即 OK


def test_openapi_ignores_local_dotenv(tmp_path, monkeypatch):
    """cwd 存在污染的 .env 时，schema 仍确定（build 内部 chdir 绕过本地 .env）。"""
    clean = build_contract_openapi()
    monkeypatch.chdir(tmp_path)
    (tmp_path / ".env").write_text('APP_AUTH_PUBLIC_PATHS=["/polluted"]\n', encoding="utf-8")
    assert build_contract_openapi() == clean, ".env 文件污染了 OpenAPI schema"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/unit/test_dump_openapi.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'scripts.dump_openapi'`

- [ ] **Step 3: 实现 dump 脚本（固定 contract profile + 清缓存）**

`scripts/dump_openapi.py`:

```python
"""DB-free 确定性导出 OpenAPI schema，供前端 codegen 稳定输入。

设计（spec §3.1/§7/§9）：
- 不读本地 .env：用固定 contract profile 覆盖影响 schema 的 APP_*。
- 清空 get_settings 的 lru_cache，避免缓存的本地配置污染。
- 不连 DB / 不起服务：仅调 create_app().openapi()。
"""
from __future__ import annotations

import argparse
import json
import os
import sys
from pathlib import Path
from typing import Any

# 固定 contract profile：只放影响 OpenAPI schema 的键，值是契约规范值（非本地环境）。
_CONTRACT_ENV: dict[str, str] = {
    "APP_NAME": "admin-platform",
    # Environment 是 Literal["local","dev","staging","production"]（core/config.py:21），无 "test"。
    "APP_ENVIRONMENT": "local",
    # 其余影响 OpenAPI 的 APP_*（如 auth_public_paths）保持后端默认，不从 .env 注入。
}


def build_contract_openapi() -> dict[str, Any]:
    """在固定 contract profile 下生成 OpenAPI schema（确定性、DB-free、不读本地 .env）。"""
    import tempfile

    from admin_platform.core.config import get_settings

    # 1. 移除所有 APP_* 环境变量 + 记录原 cwd。
    saved = {k: v for k, v in os.environ.items() if k.startswith("APP_")}
    for k in saved:
        del os.environ[k]
    original_cwd = os.getcwd()
    try:
        # 2. 注入固定 contract profile。
        os.environ.update(_CONTRACT_ENV)
        # 3. chdir 到无 .env 的临时目录——Settings 的 env_file=".env" 是相对 cwd 解析
        #    （core/config.py:57），切走后读不到本地 .env，schema 不被本地配置污染。
        with tempfile.TemporaryDirectory() as tmp:
            os.chdir(tmp)
            get_settings.cache_clear()
            from admin_platform.main import create_app

            schema = create_app().openapi()
            get_settings.cache_clear()
        return schema
    finally:
        # 4. 还原 cwd 与环境，避免影响同进程其它测试。
        os.chdir(original_cwd)
        for k in list(os.environ):
            if k.startswith("APP_"):
                del os.environ[k]
        os.environ.update(saved)
        get_settings.cache_clear()


def main() -> None:
    parser = argparse.ArgumentParser(description="导出确定性 OpenAPI schema")
    parser.add_argument(
        "--output",
        default="frontend/openapi/admin-platform.json",
        help="输出路径（相对仓库根）",
    )
    args = parser.parse_args()
    schema = build_contract_openapi()
    out = Path(args.output)
    out.parent.mkdir(parents=True, exist_ok=True)
    out.write_text(
        json.dumps(schema, ensure_ascii=False, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )
    sys.stdout.write(f"OpenAPI 已导出 → {out}\n")  # 用 stdout.write 而非 print（仓库 ruff 启用 T20）


if __name__ == "__main__":
    main()
```

> 注：若 `get_settings` 不是 `functools.lru_cache` 包裹（无 `cache_clear`），改为对应的缓存清理方式；执行前先 `rg -n "def get_settings" src/admin_platform/core/config.py` 核对。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/unit/test_dump_openapi.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 验证脚本能生成（输出到临时路径，不创建 `frontend/`）**

> ⚠️ 此 task 早于前端脚手架（Task 3）。**不要**输出到 `frontend/`——否则会先建出空 `frontend/` 目录，使 Task 3 的脚手架判断误跳过 create-vue。真正的 `frontend/openapi/admin-platform.json` 产物在 Task 4（脚手架之后）生成并提交。

Run: `uv run python scripts/dump_openapi.py --output /tmp/openapi-check.json && head -c 120 /tmp/openapi-check.json`
Expected: 打印 `OpenAPI 已导出 → /tmp/openapi-check.json`，文件以 `{` 开头含 `"openapi"`。

- [ ] **Step 6: Commit（仅脚本 + 测试，不含 frontend 产物）**

```bash
git add scripts/dump_openapi.py tests/unit/test_dump_openapi.py
git commit -m "feat(p6.0): DB-free 确定性 OpenAPI 导出脚本"
```

---

## Task 2: 后端 seed 页面组件真值源 contract

**Files:**
- Create: `tests/contracts/seed_page_components.json`
- Test: `tests/unit/test_seed_page_components.py`
- Reference: `src/admin_platform/rbac/seed.py`（`MENU_TREE`）、`src/admin_platform/domains/menu/routers.py`（`menu_type` 过滤逻辑）

- [ ] **Step 1: 写失败测试 —— fixture == MENU_TREE 中 menu_type=="C" 的 component 全集**

`tests/unit/test_seed_page_components.py`:

```python
"""seed 页面组件跨语言真值源：fixture 必须等于 MENU_TREE 中菜单(C)的 component 全集。

前端 Vitest 只读该 fixture 断言 ⊆ import.meta.glob keys（spec §5）。
后端改 seed 不同步 fixture → 本测试红，防跨语言漂移。
"""
import json
from collections.abc import Iterable
from pathlib import Path

from admin_platform.rbac.seed import MENU_TREE, SeedMenu

FIXTURE = Path("tests/contracts/seed_page_components.json")


def _collect_page_components(nodes: Iterable[SeedMenu]) -> set[str]:
    """递归收集 menu_type == 'C' 且有 component 的页面组件路径。

    MENU_TREE 是 tuple[SeedMenu, ...] frozen dataclass（seed.py:32），用属性访问、非 dict.get()。
    目录(M)的 component 是 Layout/ParentView 壳名、按钮(F)无 component，均排除。
    """
    result: set[str] = set()
    for node in nodes:
        if node.menu_type == "C" and node.component:
            result.add(node.component)
        result |= _collect_page_components(node.children)
    return result


def test_fixture_matches_seed_page_components():
    expected = _collect_page_components(MENU_TREE)
    actual = set(json.loads(FIXTURE.read_text(encoding="utf-8")))
    assert actual == expected, (
        f"seed_page_components.json 与 MENU_TREE 漂移；"
        f"缺 {expected - actual}，多 {actual - expected}"
    )
```

> 字段名已核对：`SeedMenu` 是 frozen dataclass，含 `menu_type`/`component`/`children`（`rbac/seed.py:32-45`），用属性访问。

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/unit/test_seed_page_components.py -v`
Expected: FAIL — `FileNotFoundError` 或断言失败（fixture 不存在/为空）

- [ ] **Step 3: 用脚本生成 fixture 真值**

Run（一次性生成，之后由测试守门）:

```bash
uv run python -c "
import json
from tests.unit.test_seed_page_components import _collect_page_components
from admin_platform.rbac.seed import MENU_TREE
comps = sorted(_collect_page_components(MENU_TREE))
import pathlib
pathlib.Path('tests/contracts/seed_page_components.json').write_text(
    json.dumps(comps, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')
print('页面组件数:', len(comps)); print(comps)
"
```
Expected: 打印形如 `['monitor/job/index', 'system/user/index', ...]`（仅菜单 C 的页面路径，无 `Layout`/`ParentView`）。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/unit/test_seed_page_components.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tests/contracts/seed_page_components.json tests/unit/test_seed_page_components.py
git commit -m "feat(p6.0): seed 页面组件跨语言真值源 contract"
```

---

## Task 3: 前端脚手架初始化

**Files:**
- Create: `frontend/`（create-vue 生成）、`frontend/.node-version`、`.gitignore` 追加
- Reference: spec §3 技术栈

- [ ] **Step 1: 用官方 create-vue 生成骨架**

Run（在仓库根；非交互式选项）:

```bash
test -f frontend/package.json && echo "frontend 脚手架已存在，跳过" || \
pnpm create vue@latest frontend --ts --router --pinia --vitest --playwright --eslint-with-prettier
```
Expected: 生成 `frontend/`，含 `package.json`、`src/`、`vite.config.ts`、`tsconfig.json`。（判断用 `package.json` 而非目录存在性——Task 1 可能已建出空 `frontend/openapi/`。）

> 若 create-vue 交互无法跳过，手动逐项选择：TypeScript=Yes、Router=Yes、Pinia=Yes、Vitest=Yes、E2E=Playwright、ESLint+Prettier=Yes、JSX/SSR=No。

- [ ] **Step 2: 锁 Node/pnpm 版本 + 装 Element Plus + openapi 工具**

Run:

```bash
cd /Users/songshanshan/PythonProjects/admin-platform/frontend
echo "22" > .node-version
pnpm pkg set packageManager="pnpm@10.0.0"
pnpm add element-plus
pnpm add -D openapi-typescript openapi-fetch dependency-cruiser @commitlint/cli @commitlint/config-conventional
pnpm install
```
Expected: `package.json` 含上述依赖，`pnpm-lock.yaml` 生成。

- [ ] **Step 3: 开启 TS strict 并验证构建**

确认 `frontend/tsconfig.app.json`（或 `tsconfig.json`）`compilerOptions` 含 `"strict": true`（create-vue 默认开启；若无则加）。

Run:

```bash
pnpm build
```
Expected: 构建成功，产出 `dist/`。

- [ ] **Step 4: 忽略生成产物外的噪音**

在仓库根 `.gitignore` 追加（若未含）:

```
frontend/node_modules/
frontend/dist/
frontend/.vite/
```

- [ ] **Step 5: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/package.json frontend/pnpm-lock.yaml frontend/.node-version frontend/tsconfig*.json frontend/vite.config.ts frontend/src frontend/index.html frontend/eslint.config.* .gitignore
git commit -m "feat(p6.0): 前端脚手架 create-vue + Element Plus + openapi 工具链"
```

---

## Task 4: openapi-typescript 类型生成 + drift 门

**Files:**
- Create: `frontend/src/api/generated/types.ts`（生成物）
- Modify: `frontend/package.json`（scripts）

- [ ] **Step 1: 加 codegen + drift 脚本**

Run（在 `frontend/`）:

```bash
pnpm pkg set scripts.openapi:generate="openapi-typescript ./openapi/admin-platform.json -o ./src/api/generated/types.ts"
# openapi:dump 重新从后端 DB-free 生成 schema（真值源在后端，spec §7）
pnpm pkg set scripts.openapi:dump="cd .. && uv run python scripts/dump_openapi.py --output frontend/openapi/admin-platform.json"
# openapi-drift 真门：重新 dump 后端 → 生成 TS → diff（JSON + 类型都比），后端 schema 改了 CI 会红
pnpm pkg set scripts.openapi-drift="pnpm openapi:dump && pnpm openapi:generate && git -C .. diff --exit-code -- frontend/openapi/admin-platform.json frontend/src/api/generated"
```

> ⚠️ `openapi-drift` 依赖根目录 `uv` + 后端依赖（跑 `dump_openapi.py`）。若只想本地快速重生成类型而不重 dump，用 `pnpm openapi:generate`。CI（Task 12）已加 uv setup。

- [ ] **Step 2: 从后端 dump OpenAPI 产物 + 生成类型**

Run（在 `frontend/`，`openapi:dump` 需仓库根 uv 环境）:

```bash
pnpm openapi:dump && pnpm openapi:generate && head -30 src/api/generated/types.ts
```
Expected: 生成 `openapi/admin-platform.json`（后端 dump，此处才首次落到 `frontend/`）+ `src/api/generated/types.ts`（含 `export interface paths` 与 `"/api/v1/auth/login"`）。

- [ ] **Step 3: 验证 drift 门（无改动应 exit 0）**

Run:

```bash
pnpm openapi-drift; echo "exit=$?"
```
Expected: `exit=0`（生成结果与已提交一致）。

- [ ] **Step 4: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/package.json frontend/openapi/admin-platform.json frontend/src/api/generated/types.ts
git commit -m "feat(p6.0): openapi 产物 + 类型生成 + drift 门"
```

---

## Task 5: SessionExpiredError + session emitter（最小骨架，TDD）

**Files:**
- Create: `frontend/src/api/session.ts`
- Test: `frontend/src/api/session.spec.ts`

- [ ] **Step 1: 写失败测试 —— 类型错误与事件订阅**

`frontend/src/api/session.spec.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { SessionExpiredError, onSessionExpired, emitSessionExpired } from './session'

describe('session error & emitter', () => {
  it('SessionExpiredError 是带 name 的 Error 子类', () => {
    const err = new SessionExpiredError('refresh failed')
    expect(err).toBeInstanceOf(Error)
    expect(err.name).toBe('SessionExpiredError')
    expect(err.message).toBe('refresh failed')
  })

  it('onSessionExpired 订阅者在 emit 时被调用一次', () => {
    const handler = vi.fn()
    const off = onSessionExpired(handler)
    emitSessionExpired()
    expect(handler).toHaveBeenCalledTimes(1)
    off()
    emitSessionExpired()
    expect(handler).toHaveBeenCalledTimes(1) // 退订后不再触发
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && pnpm vitest run src/api/session.spec.ts`
Expected: FAIL — 无法从 `./session` 导入。

- [ ] **Step 3: 实现 error + emitter（不依赖 stores/router）**

`frontend/src/api/session.ts`:

```typescript
/**
 * 唯一认证/刷新协调器（token 真值源）。
 * 严格不依赖 stores/router：失效经 emitter 发事件 + 抛 typed error，
 * 由 router composition root 注册的唯一订阅者处理 redirect/清 Pinia/reset 路由。
 * 见 spec §3.1/§4/§6。
 */

/** refresh 失败 / token 失效的 typed error；normalizeApiError 必须透传它不降级。 */
export class SessionExpiredError extends Error {
  constructor(message = 'session expired') {
    super(message)
    this.name = 'SessionExpiredError'
  }
}

type ExpiredHandler = () => void
const expiredHandlers = new Set<ExpiredHandler>()

/** 注册 session 失效订阅者；返回退订函数。router composition root 注册唯一一个。 */
export function onSessionExpired(handler: ExpiredHandler): () => void {
  expiredHandlers.add(handler)
  return () => expiredHandlers.delete(handler)
}

/** 触发 session 失效事件（清理副作用必达，无论哪条请求路径触发）。 */
export function emitSessionExpired(): void {
  for (const handler of expiredHandlers) handler()
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && pnpm vitest run src/api/session.spec.ts`
Expected: PASS（2 passed）

- [ ] **Step 5: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/src/api/session.ts frontend/src/api/session.spec.ts
git commit -m "feat(p6.0): SessionExpiredError + session 失效 emitter"
```

---

## Task 6: session token 持有 + single-flight refresh（TDD 核心）

**Files:**
- Modify: `frontend/src/api/session.ts`
- Modify: `frontend/src/api/session.spec.ts`

- [ ] **Step 1: 写失败测试 —— single-flight + 失效 emit + clearTokens**

向 `frontend/src/api/session.spec.ts` 追加:

```typescript
import {
  setTokens, clearTokens, hasRefresh, attachAuthHeaders, refreshOnce,
  __resetSessionForTest, __setRefreshImplForTest,
} from './session'

describe('session tokens & single-flight refresh', () => {
  beforeEach(() => {
    __resetSessionForTest()
    sessionStorage.clear()
  })

  it('setTokens 写入后 attachAuthHeaders 注入 Bearer，hasRefresh 为真', () => {
    setTokens({ accessToken: 'a1', refreshToken: 'r1' })
    const headers = new Headers()
    attachAuthHeaders(headers)
    expect(headers.get('Authorization')).toBe('Bearer a1')
    expect(hasRefresh()).toBe(true)
  })

  it('clearTokens 清空内存与 sessionStorage 的 refresh', () => {
    setTokens({ accessToken: 'a1', refreshToken: 'r1' })
    clearTokens()
    expect(hasRefresh()).toBe(false)
    const headers = new Headers()
    attachAuthHeaders(headers)
    expect(headers.get('Authorization')).toBeNull()
  })

  it('并发 refreshOnce 只发起一次刷新请求（single-flight，client/transport 共享）', async () => {
    setTokens({ accessToken: 'a0', refreshToken: 'r0' })
    const impl = vi.fn(async () => {
      await new Promise((r) => setTimeout(r, 10))
      return { accessToken: 'a1', refreshToken: 'r1' }
    })
    __setRefreshImplForTest(impl)
    const [r1, r2, r3] = await Promise.all([refreshOnce(), refreshOnce(), refreshOnce()])
    expect(impl).toHaveBeenCalledTimes(1) // 关键：跨多次调用只刷一次
    expect(r1).toBe('a1'); expect(r2).toBe('a1'); expect(r3).toBe('a1')
  })

  it('refresh 失败 → clearTokens + emit sessionExpired + 抛 SessionExpiredError', async () => {
    setTokens({ accessToken: 'a0', refreshToken: 'r0' })
    const handler = vi.fn()
    onSessionExpired(handler)
    __setRefreshImplForTest(async () => { throw new Error('401') })
    await expect(refreshOnce()).rejects.toBeInstanceOf(SessionExpiredError)
    expect(handler).toHaveBeenCalledTimes(1)
    expect(hasRefresh()).toBe(false)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && pnpm vitest run src/api/session.spec.ts`
Expected: FAIL — `setTokens` 等未导出。

- [ ] **Step 3: 实现 token 持有 + single-flight refresh**

向 `frontend/src/api/session.ts` 追加（接在文件末尾）:

```typescript
const REFRESH_STORAGE_KEY = 'admin.refresh'
const BASE = import.meta.env.VITE_API_BASE ?? ''

interface Tokens {
  accessToken: string
  refreshToken: string
}

/** refresh 实现：传入旧 refresh，返回新 token 对。运行时为裸 fetch 调 /auth/refresh（防递归）。 */
export type RefreshImpl = (refreshToken: string) => Promise<Tokens>

let accessToken: string | null = null
let inflight: Promise<string> | null = null // single-flight 句柄（模块级 → client/transport 共享）

/** 默认 refresh 实现：不挂任何拦截的裸 fetch，避免递归 401（spec §3.1）。 */
const defaultRefreshImpl: RefreshImpl = async (refreshToken) => {
  const res = await fetch(`${BASE}/api/v1/auth/refresh`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ refresh_token: refreshToken }),
  })
  if (!res.ok) throw new SessionExpiredError(`refresh ${res.status}`)
  const data = (await res.json()) as { access_token: string; refresh_token: string }
  return { accessToken: data.access_token, refreshToken: data.refresh_token }
}
let refreshImpl: RefreshImpl = defaultRefreshImpl

export function setTokens(tokens: Tokens): void {
  accessToken = tokens.accessToken
  sessionStorage.setItem(REFRESH_STORAGE_KEY, tokens.refreshToken)
}

export function clearTokens(): void {
  accessToken = null
  sessionStorage.removeItem(REFRESH_STORAGE_KEY)
}

export function hasRefresh(): boolean {
  return !!sessionStorage.getItem(REFRESH_STORAGE_KEY)
}

export function attachAuthHeaders(headers: Headers): void {
  if (accessToken) headers.set('Authorization', `Bearer ${accessToken}`)
}

/**
 * single-flight 刷新（无参，client 与 transport 共享同一 inflight）。
 * 成功 → setTokens 返回新 access；失败 → clearTokens + emit + 抛 SessionExpiredError。
 */
export function refreshOnce(): Promise<string> {
  if (inflight) return inflight
  const refreshToken = sessionStorage.getItem(REFRESH_STORAGE_KEY)
  inflight = (async () => {
    try {
      if (!refreshToken) throw new SessionExpiredError('no refresh token')
      const tokens = await refreshImpl(refreshToken)
      setTokens(tokens)
      return tokens.accessToken
    } catch (err) {
      clearTokens()
      emitSessionExpired()
      throw err instanceof SessionExpiredError ? err : new SessionExpiredError('refresh failed')
    } finally {
      inflight = null
    }
  })()
  return inflight
}

/** 仅供单测注入 mock refresh 实现。 */
export function __setRefreshImplForTest(impl: RefreshImpl): void {
  refreshImpl = impl
}

/** 仅供单测重置模块级状态。 */
export function __resetSessionForTest(): void {
  accessToken = null
  inflight = null
  refreshImpl = defaultRefreshImpl
  expiredHandlers.clear()
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && pnpm vitest run src/api/session.spec.ts`
Expected: PASS（全部 session 用例绿）

- [ ] **Step 5: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/src/api/session.ts frontend/src/api/session.spec.ts
git commit -m "feat(p6.0): session token 持有 + single-flight refresh"
```

---

## Task 7: transport.ts —— normalizeApiError 透传 + blob/multipart（TDD）

**Files:**
- Create: `frontend/src/api/transport.ts`
- Test: `frontend/src/api/transport.spec.ts`

- [ ] **Step 1: 写失败测试 —— 错误归一透传 SessionExpiredError + blob/FormData**

`frontend/src/api/transport.spec.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { normalizeApiError, normalizeResponseError, downloadBlob, uploadMultipart } from './transport'
import { SessionExpiredError, __resetSessionForTest, setTokens } from './session'

beforeEach(() => { __resetSessionForTest(); sessionStorage.clear() })

describe('normalizeApiError', () => {
  it('透传 SessionExpiredError 不降级', () => {
    const e = new SessionExpiredError()
    expect(normalizeApiError(e)).toBe(e)
  })

  it('AbortError → 归一为带 code TIMEOUT 的 ApiError', () => {
    const e = new DOMException('aborted', 'AbortError')
    const out = normalizeApiError(e)
    expect(out).not.toBeInstanceOf(SessionExpiredError)
    expect((out as { code: string }).code).toBe('TIMEOUT')
  })
})

describe('normalizeResponseError', () => {
  it('解析 RFC9457 ProblemDetail（type/title/status/detail）', async () => {
    const res = new Response(
      JSON.stringify({ type: 'admin_platform.USER_NOT_FOUND', title: 'User not found', status: 404, detail: 'id=42' }),
      { status: 404, headers: { 'Content-Type': 'application/json' } },
    )
    const err = await normalizeResponseError(res)
    expect(err).toMatchObject({
      code: 'admin_platform.USER_NOT_FOUND', status: 404, message: 'User not found', detail: 'id=42',
    })
  })

  it('非 JSON body → 用 status 兜底为 HTTP_<status>', async () => {
    const err = await normalizeResponseError(new Response('oops', { status: 502 }))
    expect(err).toMatchObject({ code: 'HTTP_502', status: 502 })
  })
})

describe('downloadBlob / uploadMultipart', () => {
  it('downloadBlob 注入 auth header 并返回 blob', async () => {
    setTokens({ accessToken: 'a1', refreshToken: 'r1' })
    const fetchMock = vi.fn(async (_url: string, init: RequestInit) => {
      expect((init.headers as Headers).get('Authorization')).toBe('Bearer a1')
      return new Response(new Blob(['hello']), { status: 200 })
    })
    vi.stubGlobal('fetch', fetchMock)
    const blob = await downloadBlob('/api/v1/posts/export')
    expect(await blob.text()).toBe('hello')
  })

  it('uploadMultipart 以 FormData 发送且不设 Content-Type（交由浏览器）', async () => {
    setTokens({ accessToken: 'a1', refreshToken: 'r1' })
    const fetchMock = vi.fn(async (_url: string, init: RequestInit) => {
      expect(init.body).toBeInstanceOf(FormData)
      expect((init.headers as Headers).has('Content-Type')).toBe(false)
      return new Response('{"imported":1}', { status: 200 })
    })
    vi.stubGlobal('fetch', fetchMock)
    const fd = new FormData()
    fd.append('file', new Blob(['x']), 'a.xlsx')
    const res = await uploadMultipart('/api/v1/posts/import', fd)
    expect(res.status).toBe(200)
  })

  it('downloadBlob 非 2xx → 抛归一 ApiError（保留 status/code）', async () => {
    setTokens({ accessToken: 'a1', refreshToken: 'r1' })
    vi.stubGlobal('fetch', vi.fn(async () => new Response(
      JSON.stringify({ type: 'file.NOT_FOUND', title: 'not found', status: 404 }),
      { status: 404, headers: { 'Content-Type': 'application/json' } },
    )))
    await expect(downloadBlob('/api/v1/files/9/download')).rejects.toMatchObject({
      code: 'file.NOT_FOUND', status: 404,
    })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && pnpm vitest run src/api/transport.spec.ts`
Expected: FAIL — 无法导入 `./transport`。

- [ ] **Step 3: 实现 transport（超时分场景 + 错误归一 + auth 经 session）**

`frontend/src/api/transport.ts`:

```typescript
/**
 * 二进制/错误通道：multipart 上传、blob 下载、统一错误归一。
 * auth header 经 session.attachAuthHeaders；不与 openapi-fetch 重复造 refresh 逻辑。
 * 见 spec §3.1。
 */
import { attachAuthHeaders, refreshOnce, SessionExpiredError } from './session'

const JSON_TIMEOUT_MS = 15_000
const BINARY_TIMEOUT_MS = 120_000 // 下载/上传更长，防 Excel 导出被误 abort（spec §3.1）

/** 归一后的业务错误形状（RFC9457 ProblemDetail 子集 + 传输级）。 */
export interface ApiError {
  code: string
  status: number
  message: string
  detail?: string
}

/** 抛出/异常侧归一：SessionExpiredError 必须透传不降级。 */
export function normalizeApiError(err: unknown): SessionExpiredError | ApiError {
  if (err instanceof SessionExpiredError) return err
  if (err instanceof DOMException && err.name === 'AbortError') {
    return { code: 'TIMEOUT', status: 0, message: '请求超时' }
  }
  if (err instanceof Error) {
    return { code: 'NETWORK', status: 0, message: err.message }
  }
  return { code: 'UNKNOWN', status: 0, message: String(err) }
}

/**
 * 非 2xx Response 侧归一：优先解析 RFC9457 ProblemDetail（spec §3.1）。
 * blob/multipart 接口失败时后端仍返回 JSON ProblemDetail，这里读出 type/title/status/detail。
 */
export async function normalizeResponseError(res: Response): Promise<ApiError> {
  let body: unknown = null
  try {
    body = await res.clone().json()
  } catch {
    // 非 JSON（纯文本/空）→ 下面用 status 兜底
  }
  if (body && typeof body === 'object') {
    const pd = body as Record<string, unknown>
    return {
      code: typeof pd.type === 'string' ? pd.type : `HTTP_${res.status}`,
      status: typeof pd.status === 'number' ? pd.status : res.status,
      message: typeof pd.title === 'string' ? pd.title : res.statusText || '请求失败',
      detail: typeof pd.detail === 'string' ? pd.detail : undefined,
    }
  }
  return { code: `HTTP_${res.status}`, status: res.status, message: res.statusText || '请求失败' }
}

function withTimeout(ms: number): { signal: AbortSignal; done: () => void } {
  const ctrl = new AbortController()
  const id = setTimeout(() => ctrl.abort(), ms)
  return { signal: ctrl.signal, done: () => clearTimeout(id) }
}

const BASE = import.meta.env.VITE_API_BASE ?? ''

export async function downloadBlob(path: string): Promise<Blob> {
  const send = async (): Promise<Response> => {
    const headers = new Headers()
    attachAuthHeaders(headers)
    const t = withTimeout(BINARY_TIMEOUT_MS)
    try {
      return await fetch(`${BASE}${path}`, { headers, signal: t.signal })
    } finally {
      t.done()
    }
  }
  let res = await send()
  if (res.status === 401) {
    await refreshOnce() // 与 JSON 通道共享 single-flight；失败抛 SessionExpiredError
    res = await send()
  }
  if (!res.ok) throw await normalizeResponseError(res) // RFC9457 归一，保留 status/code
  return await res.blob()
}

export async function uploadMultipart(path: string, form: FormData): Promise<Response> {
  // 不手动设 Content-Type：浏览器会带 multipart boundary。
  const send = async (): Promise<Response> => {
    const headers = new Headers()
    attachAuthHeaders(headers)
    const t = withTimeout(BINARY_TIMEOUT_MS)
    try {
      return await fetch(`${BASE}${path}`, { method: 'POST', headers, body: form, signal: t.signal })
    } finally {
      t.done()
    }
  }
  let res = await send()
  if (res.status === 401) {
    await refreshOnce() // 与 JSON 通道共享 single-flight
    res = await send()
  }
  // import 业务通道是 200+summary.errors（spec），故 200 即使含行级错误也放行；
  // 仅传输级失败（409 并发 / 413 超大 / 4xx/5xx）归一抛出。
  if (!res.ok) throw await normalizeResponseError(res)
  return res
}

export const _timeouts = { JSON_TIMEOUT_MS, BINARY_TIMEOUT_MS }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && pnpm vitest run src/api/transport.spec.ts`
Expected: PASS（4 passed）

- [ ] **Step 5: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/src/api/transport.ts frontend/src/api/transport.spec.ts
git commit -m "feat(p6.0): transport blob/multipart + normalizeApiError 透传"
```

---

## Task 7B: openapi-fetch client（middleware→session 401 重放）+ 跨通道 single-flight 集成（TDD）

**Files:**
- Create: `frontend/src/api/client.ts`
- Test: `frontend/src/api/cross-channel.spec.ts`

> 这是 P6.0 最高价值 spike：验证 JSON 通道（client）与二进制通道（transport）同轮 401 **共享同一 single-flight refresh**、失败都触达**唯一** `handleSessionExpired`（spec §7 关键路径必测）。

- [ ] **Step 1: 写失败测试 —— 跨通道 single-flight + 失效统一**

`frontend/src/api/cross-channel.spec.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import {
  __resetSessionForTest, __setRefreshImplForTest, setTokens, onSessionExpired,
} from './session'
import { apiClient } from './client'
import { downloadBlob } from './transport'

beforeEach(() => {
  __resetSessionForTest()
  sessionStorage.clear()
  vi.unstubAllGlobals()
})

describe('跨通道 single-flight / 失效统一出口', () => {
  it('JSON(client) 与 blob(transport) 同轮 401 只刷新一次', async () => {
    setTokens({ accessToken: 'old', refreshToken: 'r0' })
    const impl = vi.fn(async () => {
      await new Promise((r) => setTimeout(r, 5))
      return { accessToken: 'new', refreshToken: 'r1' }
    })
    __setRefreshImplForTest(impl)

    // 带旧 token 的请求 401；刷新后带新 token 的请求 200。
    const fetchMock = vi.fn(async (input: RequestInfo | URL, init?: RequestInit) => {
      const req = input instanceof Request ? input : new Request(String(input), init)
      const auth = req.headers.get('Authorization') ?? ''
      if (auth.includes('old')) return new Response(null, { status: 401 })
      return new Response('{}', { status: 200, headers: { 'Content-Type': 'application/json' } })
    })
    vi.stubGlobal('fetch', fetchMock)

    await Promise.all([
      apiClient.GET('/api/v1/users' as never, {} as never),
      downloadBlob('/api/v1/files/1/download'),
    ])
    expect(impl).toHaveBeenCalledTimes(1) // 跨通道共享同一 inflight
  })

  it('refresh 失败时两条通道都触达唯一 handleSessionExpired（emit 一次）', async () => {
    setTokens({ accessToken: 'old', refreshToken: 'r0' })
    const handler = vi.fn()
    onSessionExpired(handler)
    __setRefreshImplForTest(async () => { throw new Error('401') })
    vi.stubGlobal('fetch', vi.fn(async () => new Response(null, { status: 401 })))

    await Promise.allSettled([
      apiClient.GET('/api/v1/users' as never, {} as never),
      downloadBlob('/api/v1/files/1/download'),
    ])
    expect(handler).toHaveBeenCalledTimes(1) // single-flight 失败只 emit 一次
  })

  it('POST(带 body)401 重放保留 body（clone 未被消费）', async () => {
    setTokens({ accessToken: 'old', refreshToken: 'r0' })
    __setRefreshImplForTest(async () => ({ accessToken: 'new', refreshToken: 'r1' }))
    const bodies: string[] = []
    const fetchMock = vi.fn(async (input: RequestInfo | URL, init?: RequestInit) => {
      const req = input instanceof Request ? input : new Request(String(input), init)
      const auth = req.headers.get('Authorization') ?? ''
      bodies.push(await req.clone().text())
      return auth.includes('old')
        ? new Response(null, { status: 401 })
        : new Response('{}', { status: 200, headers: { 'Content-Type': 'application/json' } })
    })
    vi.stubGlobal('fetch', fetchMock)

    await apiClient.POST('/api/v1/posts' as never, { body: { code: 'A', name: '岗位' } } as never)
    expect(bodies).toHaveLength(2)
    expect(bodies[0]).toContain('岗位')
    expect(bodies[1]).toContain('岗位') // 重放 body 仍完整
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && pnpm vitest run src/api/cross-channel.spec.ts`
Expected: FAIL — 无法从 `./client` 导入 `apiClient`。

- [ ] **Step 3: 实现 openapi-fetch client（middleware 接 session）**

`frontend/src/api/client.ts`:

```typescript
/**
 * openapi-fetch 类型化 JSON CRUD 客户端。
 * middleware 接 session：onRequest 注入 auth header；onResponse 401（非 auth 端点）→ refreshOnce 重放。
 * 见 spec §3.1。
 */
import createClient, { type Middleware } from 'openapi-fetch'
import type { paths } from './generated/types'
import { attachAuthHeaders, refreshOnce } from './session'

const BASE = import.meta.env.VITE_API_BASE ?? ''
const AUTH_PATHS = [
  '/api/v1/auth/login', '/api/v1/auth/refresh', '/api/v1/auth/logout', '/api/v1/auth/captcha',
]

function isAuthPath(url: string): boolean {
  return AUTH_PATHS.some((p) => url.includes(p))
}

// 保存每个请求的 clone（onRequest 时 body 未消费），供 401 重放。
// 直接 new Request(request) 重放有 body 的请求会因 body 已被发送消费而失败（openapi-fetch 不自动 clone）。
const pendingClones = new Map<string | number, Request>()

const authMiddleware: Middleware = {
  async onRequest({ request, id }) {
    attachAuthHeaders(request.headers)
    pendingClones.set(id, request.clone()) // clone 持有未消费 body
    return request
  },
  async onResponse({ request, response, id }) {
    const original = pendingClones.get(id)
    pendingClones.delete(id)
    if (response.status === 401 && !isAuthPath(request.url) && original) {
      await refreshOnce() // 共享 single-flight；失败抛 SessionExpiredError → 调用方 reject
      const retry = original.clone() // 带完整 body 的全新请求
      attachAuthHeaders(retry.headers)
      return fetch(retry)
    }
    return response
  },
  async onError({ id }) {
    pendingClones.delete(id) // 防泄漏
  },
}

export const apiClient = createClient<paths>({ baseUrl: BASE })
apiClient.use(authMiddleware)
```

> openapi-fetch middleware 的 `onRequest`/`onResponse`/`onError` 签名与 ctx `id` 字段以安装版本为准；执行前 `cat node_modules/openapi-fetch/dist/index.d.ts | grep -A5 Middleware` 核对。若该版本无 `id`，改用 `WeakMap<Request, Request>` 以 request 引用为 key。若 `request.url` 不可直接读，用 `new URL(request.url).pathname`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && pnpm vitest run src/api/cross-channel.spec.ts`
Expected: PASS（2 passed）。若 openapi-fetch 重放 API 形态不同，按其 `onResponse` 返回值约定微调（返回新 `Response`）。

- [ ] **Step 5: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/src/api/client.ts frontend/src/api/cross-channel.spec.ts
git commit -m "feat(p6.0): openapi-fetch client 401 重放 + 跨通道 single-flight 集成"
```

---

## Task 8: 动态路由转换器 + seed 页面组件 ⊆ glob contract（TDD）

**Files:**
- Create: `frontend/src/router/dynamic-routes.ts`
- Create: `frontend/src/layouts/Layout.vue`、`frontend/src/layouts/ParentView.vue`（最小壳）
- Create: 占位页 `frontend/src/views/system/user/index.vue` 等（按 fixture）
- Test: `frontend/src/router/dynamic-routes.spec.ts`、`frontend/src/router/seed-coverage.spec.ts`

- [ ] **Step 1: 写失败测试 —— 转换器映射 + 全 seed 页面组件被 glob 覆盖**

`frontend/src/router/dynamic-routes.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { toRoutes, SHELL_ALLOWLIST } from './dynamic-routes'

const sample = [
  {
    name: 'System', path: '/system', component: 'Layout', hidden: false,
    redirect: 'noRedirect', alwaysShow: true,
    meta: { title: '系统管理', icon: 'system', noCache: false, link: null },
    children: [
      {
        name: 'User', path: 'user', component: 'system/user/index', hidden: false,
        redirect: null, alwaysShow: false,
        meta: { title: '用户管理', icon: 'user', noCache: false, link: null },
      },
    ],
  },
]

describe('toRoutes', () => {
  it('Layout/ParentView 映射到壳，页面 component 映射到 views', () => {
    const routes = toRoutes(sample)
    expect(routes[0].path).toBe('/system')
    expect(SHELL_ALLOWLIST).toContain('Layout')
    expect(routes[0].children?.[0].path).toBe('user')
    // 页面组件以函数形式懒加载（import.meta.glob 命中）
    expect(typeof routes[0].children?.[0].component).toBe('function')
  })

  it('未知 component 抛错（fail fast，不静默忽略）', () => {
    const bad = [{ ...sample[0], children: [{ ...sample[0].children![0], component: 'no/such/page' }] }]
    expect(() => toRoutes(bad)).toThrow(/no\/such\/page/)
  })
})
```

`frontend/src/router/seed-coverage.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import seedPages from '../../../tests/contracts/seed_page_components.json'
import { pageComponentKeys } from './dynamic-routes'

describe('seed 页面组件覆盖契约', () => {
  it('seed_page_components.json ⊆ import.meta.glob 页面 key 集合（大小写精确）', () => {
    const globKeys = new Set(pageComponentKeys())
    const missing = (seedPages as string[]).filter((c) => !globKeys.has(c))
    expect(missing, `缺少页面组件: ${missing.join(', ')}`).toEqual([])
  })
})
```

> 路径 `../../../tests/contracts/seed_page_components.json` 假设 `frontend/` 与仓库根同级；执行前确认相对深度，必要时在 `vite.config.ts` 配 `resolve.alias` 指向该 fixture。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && pnpm vitest run src/router/`
Expected: FAIL — 无法导入 `./dynamic-routes`。

- [ ] **Step 3: 实现壳组件 + 转换器（glob 白名单）**

`frontend/src/layouts/Layout.vue`:

```vue
<script setup lang="ts">
// 顶层布局壳：侧边栏/面包屑/内容区在 P6.1 填充，这里先放 router-view 占位。
</script>
<template>
  <router-view />
</template>
```

`frontend/src/layouts/ParentView.vue`:

```vue
<script setup lang="ts">
// 嵌套目录的中间壳：仅渲染子路由。
</script>
<template>
  <router-view />
</template>
```

`frontend/src/views/system/user/index.vue`（按 fixture 为每个 `menu_type=="C"` 页面建最小占位；此处示范一个，其余同构）:

```vue
<script setup lang="ts">
// P6.0 占位页，P6.2 填充真实 CRUD。
</script>
<template>
  <div>system/user/index</div>
</template>
```

`frontend/src/router/dynamic-routes.ts`:

```typescript
/**
 * 若依菜单树 → Vue Router 路由配置。
 * component 字符串作 key 经 import.meta.glob 白名单映射；缺页 fail fast。
 * 见 spec §5。
 */
import type { RouteRecordRaw } from 'vue-router'
import Layout from '@/layouts/Layout.vue'
import ParentView from '@/layouts/ParentView.vue'

/** 路由壳组件 allowlist（非页面，不参与 seed 覆盖契约）。 */
export const SHELL_ALLOWLIST = ['Layout', 'ParentView'] as const
const SHELLS: Record<string, unknown> = { Layout, ParentView }

/** 所有页面组件：views 下的 index.vue，key 规范化为 'system/user/index' 形态。 */
const pageModules = import.meta.glob('@/views/**/index.vue')

/** 把 '/src/views/system/user/index.vue' → 'system/user/index'。 */
function normalizeKey(globPath: string): string {
  return globPath.replace(/^.*\/views\//, '').replace(/\.vue$/, '')
}

const pageMap = new Map<string, () => Promise<unknown>>()
for (const [globPath, loader] of Object.entries(pageModules)) {
  pageMap.set(normalizeKey(globPath), loader as () => Promise<unknown>)
}

/** 暴露页面 key 集合，供 seed 覆盖契约测试断言。 */
export function pageComponentKeys(): string[] {
  return [...pageMap.keys()]
}

interface RouterNode {
  name: string
  path: string
  component: string
  redirect: string | null
  hidden: boolean
  alwaysShow: boolean
  meta: Record<string, unknown>
  children?: RouterNode[]
}

function resolveComponent(component: string): unknown {
  if (component in SHELLS) return SHELLS[component]
  const loader = pageMap.get(component)
  if (!loader) {
    throw new Error(`动态路由 component 无对应前端页面（缺页 fail fast）: ${component}`)
  }
  return loader
}

export function toRoutes(nodes: RouterNode[]): RouteRecordRaw[] {
  return nodes.map((node) => {
    const record: RouteRecordRaw = {
      path: node.path,
      name: node.name,
      component: resolveComponent(node.component) as RouteRecordRaw['component'],
      meta: { ...node.meta, hidden: node.hidden },
      ...(node.redirect && node.redirect !== 'noRedirect' ? { redirect: node.redirect } : {}),
    }
    if (node.children?.length) record.children = toRoutes(node.children)
    return record
  })
}
```

> 确认 `frontend/` 已配 `@` → `src` 别名（create-vue 默认配）。为每个 `seed_page_components.json` 中的页面建占位 `index.vue`，否则 seed 覆盖测试会红（这正是契约目的）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && pnpm vitest run src/router/`
Expected: PASS（转换器 + seed 覆盖全绿）。若 seed 覆盖报缺页，按报错补对应占位 `index.vue`。

- [ ] **Step 5: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/src/router/dynamic-routes.ts frontend/src/router/*.spec.ts frontend/src/layouts frontend/src/views
git commit -m "feat(p6.0): 动态路由转换器 + seed 页面组件覆盖契约"
```

---

## Task 9: dependency-cruiser 分层机检（含 api→stores/router 负向）

**Files:**
- Create: `frontend/.dependency-cruiser.cjs`
- Modify: `frontend/package.json`（scripts）
- Test: 临时违规文件验证规则生效

- [ ] **Step 1: 写依赖方向规则**

`frontend/.dependency-cruiser.cjs`:

```javascript
/** 前端分层硬约束（对标后端 import-linter）。见 spec §4。 */
module.exports = {
  forbidden: [
    {
      name: 'api-no-stores',
      comment: 'api 层（含 session）禁止 import stores',
      severity: 'error',
      from: { path: '^src/api/' },
      to: { path: '^src/stores/' },
    },
    {
      name: 'api-no-router',
      comment: 'api 层（含 session）禁止 import router（失效经 emitter+typed error 上抛）',
      severity: 'error',
      from: { path: '^src/api/' },
      to: { path: '^src/router/' },
    },
    {
      name: 'no-views-import-router',
      comment: 'views 禁止 import 本地 router singleton（只用 vue-router 的 useRouter）',
      severity: 'error',
      from: { path: '^src/views/' },
      to: { path: '^src/router/' },
    },
    {
      name: 'components-no-views',
      comment: '通用组件禁止 import views',
      severity: 'error',
      from: { path: '^src/components/' },
      to: { path: '^src/views/' },
    },
    {
      name: 'no-circular',
      comment: '禁止循环依赖',
      severity: 'error',
      from: {},
      to: { circular: true },
    },
  ],
  options: {
    doNotFollow: { path: 'node_modules' },
    tsConfig: { fileName: 'tsconfig.app.json' },
    enhancedResolveOptions: { exportsFields: ['exports'], conditionNames: ['import', 'require'] },
  },
}
```

- [ ] **Step 2: 加 depcruise 脚本**

Run（在 `frontend/`）:

```bash
pnpm pkg set scripts.depcruise="depcruise src --config .dependency-cruiser.cjs"
```

- [ ] **Step 3: 跑 depcruise 确认当前代码通过**

Run: `cd frontend && pnpm depcruise; echo "exit=$?"`
Expected: `exit=0`（现有 api/session.ts、transport.ts 不依赖 stores/router）。

- [ ] **Step 4: 负向验证 —— 临时违规必须被拦**

Run:

```bash
cd frontend
printf "import { useRouter } from '@/router'\nexport const x = useRouter\n" > src/api/__violation.ts
pnpm depcruise; echo "exit=$?"
rm src/api/__violation.ts
```
Expected: 报 `api-no-router` error 且 `exit=1`（删除后恢复）。

- [ ] **Step 5: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/.dependency-cruiser.cjs frontend/package.json
git commit -m "feat(p6.0): dependency-cruiser 分层机检 + api→stores/router 负向规则"
```

---

## Task 10: 本地提交门（接入根 pre-commit 框架，避免 husky hooksPath 冲突）

**Files:**
- Modify: `.pre-commit-config.yaml`（仓库根，追加 frontend local hooks）
- Create: `frontend/commitlint.config.cjs`

> 仓库根已用 pre-commit 框架（hooks 装到 `.git/hooks/`）。前端若引入 husky 会设 `core.hooksPath=.husky`，**覆盖并使根 pre-commit 失效**（Codex 第 1 轮 review 风险点）。故前端提交门**接入同一 pre-commit 框架**，不引 husky/lint-staged。commitlint CLI 已在 Task 3 装为 frontend devDep。

- [ ] **Step 1: 写 commitlint 配置**

Run:

```bash
echo "module.exports = { extends: ['@commitlint/config-conventional'] }" > /Users/songshanshan/PythonProjects/admin-platform/frontend/commitlint.config.cjs
```

- [ ] **Step 2: 在根 `.pre-commit-config.yaml` 的 `repos:` 末尾追加 frontend local hooks**

```yaml
  - repo: local
    hooks:
      - id: frontend-eslint
        name: frontend-eslint
        language: system
        entry: bash -c 'cd frontend && pnpm lint'
        files: ^frontend/.*\.(ts|vue)$
        pass_filenames: false
      - id: frontend-commitlint
        name: frontend-commitlint
        language: system
        entry: frontend/node_modules/.bin/commitlint --config frontend/commitlint.config.cjs --edit
        stages: [commit-msg]
        pass_filenames: true
```

- [ ] **Step 3: 安装 pre-commit 的 commit-msg hook 类型**

Run:

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
uv run pre-commit install --hook-type pre-commit --hook-type commit-msg
git config --get core.hooksPath || echo "(core.hooksPath 未设，hooks 在 .git/hooks —— 无 husky 冲突)"
```
Expected: 安装成功；`core.hooksPath` 为空（确认无 husky 占用）。

- [ ] **Step 4: 验证真实 git hook 触发（非仅直跑 commitlint）**

Run:

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git commit --allow-empty -m "随便写的" 2>&1 | grep -qiE "commitlint|subject|type-enum" && echo "commit-msg hook 已拦截 ✓" || echo "未拦截 ✗"
```
Expected: `commit-msg hook 已拦截 ✓`（非规范信息被真实 git hook 拒绝，证明 hook 真正接入）。

- [ ] **Step 5: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add .pre-commit-config.yaml frontend/commitlint.config.cjs
git commit -m "chore(p6.0): 前端提交门接入根 pre-commit（eslint + commitlint）"
```

---

## Task 11: 登录页壳 + handleSessionExpired 订阅注册（bootstrap 前置）

**Files:**
- Create: `frontend/src/views/login/index.vue`
- Create: `frontend/src/stores/auth.ts`、`frontend/src/stores/permission.ts`
- Modify: `frontend/src/main.ts`、`frontend/src/router/index.ts`
- Test: `frontend/src/stores/session-expiry.spec.ts`

- [ ] **Step 1: 写失败测试 —— 失效订阅者清 store**

`frontend/src/stores/session-expiry.spec.ts`:

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { setActivePinia, createPinia } from 'pinia'
import { usePermissionStore } from './permission'
import { registerSessionExpiryHandler } from './session-expiry'
import { emitSessionExpired, __resetSessionForTest } from '@/api/session'

beforeEach(() => { setActivePinia(createPinia()); __resetSessionForTest() })

describe('session 失效统一出口', () => {
  it('emit sessionExpired → 清 permission store 权限', () => {
    const perm = usePermissionStore()
    perm.setPermissions(['system:user:list'])
    expect(perm.has('system:user:list')).toBe(true)

    const redirect = vi.fn()
    registerSessionExpiryHandler({ redirectToLogin: redirect })
    emitSessionExpired()

    expect(perm.has('system:user:list')).toBe(false)
    expect(redirect).toHaveBeenCalledTimes(1)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && pnpm vitest run src/stores/session-expiry.spec.ts`
Expected: FAIL — 无法导入 `./permission` / `./session-expiry`。

- [ ] **Step 3: 实现 permission store + 失效订阅注册**

`frontend/src/stores/permission.ts`:

```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'

/** 按钮权限标识集合（getInfo.permissions），超管含 '*:*:*'。spec §5。 */
export const usePermissionStore = defineStore('permission', () => {
  const perms = ref<Set<string>>(new Set())
  function setPermissions(list: string[]): void {
    perms.value = new Set(list)
  }
  function has(code: string): boolean {
    return perms.value.has('*:*:*') || perms.value.has(code)
  }
  function reset(): void {
    perms.value = new Set()
  }
  return { perms, setPermissions, has, reset }
})
```

`frontend/src/stores/session-expiry.ts`:

```typescript
/**
 * session 失效统一出口（spec §6）：bootstrap 首次 refresh 之前注册唯一订阅者。
 * stores 层，允许依赖 stores；redirect 由调用方（main.ts/router）注入，避免 stores 直依赖 router。
 *
 * ⚠️ P6.0 范围边界（对 spec §6 全集的「子集」，因依赖项尚未落地，非静默降级）：
 *   spec §6 完整失效语义 = 清 auth/permission/menu store + reset 动态路由 + redirect。
 *   P6.0 尚未引入 menu store、尚未 addRoute 动态路由（均在 P6.1），故 P6.0 handler 仅
 *   清 permission + redirect。P6.1 落地动态路由/menu 时，在本同一 handler 内追加
 *   menuStore.reset() 与 resetDynamicRoutes()，结构不变。
 */
import { onSessionExpired } from '@/api/session'
import { usePermissionStore } from './permission'

export interface SessionExpiryDeps {
  redirectToLogin: () => void
}

let registered = false

export function registerSessionExpiryHandler(deps: SessionExpiryDeps): void {
  if (registered) return // 唯一订阅者
  registered = true
  onSessionExpired(() => {
    usePermissionStore().reset()
    deps.redirectToLogin()
  })
}
```

`frontend/src/stores/auth.ts`:

```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { hasRefresh } from '@/api/session'

/** 认证状态（读 session token 真值源供 UI）。spec §3.1。 */
export const useAuthStore = defineStore('auth', () => {
  const ready = ref(false)
  function isLoggedIn(): boolean {
    return hasRefresh()
  }
  return { ready, isLoggedIn }
})
```

`frontend/src/views/login/index.vue`:

```vue
<script setup lang="ts">
// P6.0 登录页壳；表单/验证码/提交在 P6.1 填充。
</script>
<template>
  <div class="login-shell">登录（P6.1 填充）</div>
</template>
```

- [ ] **Step 4: 在 main.ts 注册唯一订阅者（早于 bootstrap）**

`frontend/src/main.ts`（在 pinia 安装后、首次任何请求前加）:

```typescript
import { registerSessionExpiryHandler } from '@/stores/session-expiry'
import router from '@/router'

// 唯一 session 失效订阅者，必须先于 bootstrap 首次 refresh 注册（spec §6 订阅时序）。
registerSessionExpiryHandler({
  redirectToLogin: () => { void router.replace('/login') },
})
```

确认 `frontend/src/router/index.ts` 有静态 `/login` 路由（addRoute 前即可达）:

```typescript
const routes = [{ path: '/login', name: 'Login', component: () => import('@/views/login/index.vue') }]
```

- [ ] **Step 5: 跑测试确认通过 + 构建**

Run:

```bash
cd frontend && pnpm vitest run src/stores/session-expiry.spec.ts && pnpm build
```
Expected: 测试 PASS，构建成功。

- [ ] **Step 6: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add frontend/src/stores frontend/src/views/login frontend/src/main.ts frontend/src/router/index.ts
git commit -m "feat(p6.0): 登录页壳 + session 失效统一出口订阅"
```

---

## Task 12: CI 工作流（6 门）

**Files:**
- Create: `.github/workflows/frontend.yml`

- [ ] **Step 1: 写 CI 工作流**

`.github/workflows/frontend.yml`:

```yaml
name: frontend
on:
  pull_request:
    # 后端 router/schema 改动也影响 OpenAPI → 必须触发 frontend drift 门（spec §7）
    paths:
      - 'frontend/**'
      - 'src/admin_platform/**'
      - 'scripts/dump_openapi.py'
      - 'tests/contracts/seed_page_components.json'
      - 'pyproject.toml'
      - 'uv.lock'
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'src/admin_platform/**'
      - 'scripts/dump_openapi.py'
      - 'pyproject.toml'
      - 'uv.lock'

jobs:
  frontend-gates:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.14"   # 与后端 ci.yml 一致，确保 uv sync 有 Python（Codex 第3轮风险3）
      - name: 装后端依赖（供 openapi-drift 重新 dump 后端 schema）
        working-directory: .
        run: uv sync --frozen
      - uses: pnpm/action-setup@v4
        with: { version: 10 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm, cache-dependency-path: frontend/pnpm-lock.yaml }
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm type-check
      - run: pnpm test:unit
      - run: pnpm build
      - run: pnpm depcruise
      - run: pnpm openapi-drift   # 重新 dump 后端 schema → 生成类型 → diff（真门，spec §7）
```

> 确认 `frontend/package.json` 的 `type-check`/`test:unit`/`lint` 脚本名与 create-vue 生成的一致（create-vue 通常生成 `type-check`、`test:unit`、`lint`）。若名称不同，改 YAML 对应行。

- [ ] **Step 2: 本地预演全部 6 门**

Run:

```bash
cd frontend
pnpm lint && pnpm type-check && pnpm test:unit && pnpm build && pnpm depcruise && pnpm openapi-drift
echo "all gates exit=$?"
```
Expected: `all gates exit=0`。

- [ ] **Step 3: Commit**

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
git add .github/workflows/frontend.yml
git commit -m "ci(p6.0): 前端 6 门 CI 工作流"
```

---

## Task 13: P6.0 验收 + 文档同步

**Files:**
- Modify: `CLAUDE.md`（进度行）、`docs/specs/2026-06-11-p6-frontend-design.md`（P6.0 标 ✓）

- [ ] **Step 1: 全门绿验收**

Run（前端 6 门 + 后端相关测试）:

```bash
cd /Users/songshanshan/PythonProjects/admin-platform
uv run pytest tests/unit/test_dump_openapi.py tests/unit/test_seed_page_components.py -v
cd frontend && pnpm lint && pnpm type-check && pnpm test:unit && pnpm build && pnpm depcruise && pnpm openapi-drift
git -C .. status --porcelain   # 应只剩预期 P6.0 产物，无脚手架/hook 验证残留（Codex 第3轮建议）
```
Expected: 后端 2 文件全绿；前端 6 门全绿；`git status` 无非预期残留文件。

- [ ] **Step 2: 同步文档（drift 视为 bug）**

在 `docs/specs/2026-06-11-p6-frontend-design.md` §8 把 P6.0 标 ✓；在 `CLAUDE.md` 当前阶段表加「P6.0 前端工程基线 ✓」一行。（按实际句式增量编辑，不重复其它行。）

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md docs/specs/2026-06-11-p6-frontend-design.md
git commit -m "docs(p6.0): P6.0 工程基线验收 + 进度同步"
```

---

## 验收清单（P6.0 Definition of Done）

- [ ] 后端 `scripts/dump_openapi.py` DB-free 确定性，污染 env 测试绿
- [ ] `seed_page_components.json` 由后端测试守门（仅 `menu_type=="C"`）
- [ ] 前端脚手架可 `pnpm build`，TS strict
- [ ] `openapi-typescript` 生成类型 + `openapi-drift` 门绿
- [ ] `session.ts`：single-flight refresh + emitter + `SessionExpiredError`（4 用例绿）
- [ ] `transport.ts`：blob/multipart + 401 重放 + `normalizeApiError` 透传 SessionExpiredError（4 用例绿）
- [ ] `client.ts`：openapi-fetch 401 重放 + **跨通道 single-flight**（JSON+blob 同轮 401 只刷一次 / 失败 emit 一次，集成 2 用例绿）
- [ ] 动态路由转换器 + seed 页面组件 ⊆ glob 覆盖契约绿 + 缺页 fail fast
- [ ] `dependency-cruiser`：`api→stores/router` 负向规则可拦（exit=1 验证过）
- [ ] 本地提交门 husky/lint-staged/commitlint 生效
- [ ] session 失效统一出口订阅者清 permission store + redirect（用例绿）
- [ ] CI 6 门工作流本地预演 exit=0
